{
	"name": "backend",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"docker:up": "docker compose -f ../docker-compose.test.yml up -d",
		"docker:down": "docker compose -f ../docker-compose.test.yml down",
		"test:integration": "npm run docker:up && NODE_ENV=test DATABASE_URL=postgresql://prisma:prisma@localhost:5433npx prisma migrate deploy --schema=./prisma/schema.prisma && NODE_ENV=test DATABASE_URL=postgresql://prisma:prisma@localhost:5433tests jest --runInBand && npm run docker:down",
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "ts-node index.ts",
		"build": "tsc",
		"start": "node dist/index.js",
		"seed": "ts-node seed.ts",
		"db:generate": "prisma generate",
		"db:push": "prisma db push",
		"db:studio": "prisma studio"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"@prisma/client": "^6.12.0",
		"@types/cors": "^2.8.19",
		"cors": "^2.8.5",
		"dotenv": "^17.2.0",
		"express": "^4.21.2",
		"ml": "^8.0.0",
		"pdfkit": "^0.17.1",
		"pg": "^8.16.3",
		"socket.io": "^4.8.1",
		"uuid": "^11.1.0"
	},
	"devDependencies": {
		"@types/express": "^5.0.3",
		"@types/jest": "^30.0.0",
		"@types/node": "^24.0.14",
		"@types/pg": "^8.15.4",
		"@types/socket.io": "^3.0.1",
		"@types/supertest": "^6.0.3",
		"@types/uuid": "^10.0.0",
		"jest": "^30.0.4",
		"supertest": "^7.1.3",
		"ts-jest": "^29.4.0",
		"ts-node": "^10.9.2",
		"typescript": "^5.8.3"
	}
}
